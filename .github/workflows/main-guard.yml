name: main-guard
on:
  push:
    branches: [main]
jobs:
  main-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Require merge commit
        shell: bash
        run: test "$(git rev-list --parents -n 1 HEAD | wc -w)" -ge 3
